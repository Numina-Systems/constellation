# Constellation integrity and lifecycle remediation — execution ledger

> **RESTORATION NOTICE (2026-09-09):** the Package C worker overwrote this file with a single entry, losing prior content. The parent restored this ledger from session records and review notifications. Entries marked *(reconstructed)* paraphrase lost wording; facts were verified against reviewer notifications. This ledger is APPEND-ONLY via anchored edits — never overwrite.

**Plan:** `/home/scarndp/.local/share/polytoken/sessions/0amazg-skew/plan-001.md`  
**Approved scope:** R01–R17, verified MCP/provider hardening, AC.1–AC.21  
**Task branch:** `remediation/integrity-lifecycle-20260908`  
**Started:** 2026-09-08  
**Parent orchestrator:** execute facet (started on `codex/gpt-5.6-terra`; operator switched parent model to `zai/glm-5.3-flash` 2026-09-09)  

## Safety rail

- No push, PR, deployment, production migration, or merge to `main` is authorized.
- Preserve pre-existing untracked `AGENTS.md`, the 2026-09-08 source review, the diary sync-conflict design file, and `docs/implementation-plans/2026-05-16-continuation-refinements/`.
- A pre-existing tracked deletion of `CLAUDE.md` was observed after branch creation; it is outside this task and must not be staged or reverted by remediation workers.
- Existing migrations are immutable. The sequential schema lease must recheck the current maximum migration number before allocating a new file.
- Integration tests may only use the Phase 0 isolated database harness with explicit `TEST_DATABASE_ADMIN_URL`; no `DATABASE_URL` fallback and no local secret-bearing config.
- Live provider/API tests are opt-in only. Deno and PostgreSQL gates are required evidence where named by the approved plan; an unavailable safe prerequisite blocks the relevant acceptance criteria.

## Model routing (operator overrides on record)

- 2026-09-08: direct parent orchestration supersedes the original workflow-lead/GLM delegate requirement. Implementation/test/docs workers: `general-purpose` on `codex/gpt-5.6-luna(high)`. Launch failures are reported, never silently substituted.
- 2026-09-09: reviews use `zai/glm-5.3-flash(high)` (operator: "don't use astra for every review"). The historical Astra preflight/Phase-0 reviews below occurred before this override.

## Lease and phase status

| Phase/package | State | File lease | Dependency | Worker/model | Tests/evidence | Review disposition |
|---|---|---|---|---|---|---|
| Preflight plan review | complete | read-only full plan/repo | none | `plan-reviewer:astra-plan-review-3`, `plan-reviewer:astra-plan-rereview` / `codex/gpt-6-astra(high)` | Astra repeat pass: no findings | findings reconciled; repeat passed |
| 0 — test/contract/transaction foundation | ACCEPTED (3 low + 4 info residuals documented; DB-backed evidence pending endpoint) | `src/testing/**`, test scripts, composition seam, persistence transaction types/adapter, migration 015, setup-only migration of legacy DB suites | preflight review | `phase0-foundation`, `phase0-remediate-review`, `phase0-remediate-round2`, `phase0-remediate-round3` / `codex/gpt-5.6-luna(high)` | build pass; `test:unit` 28/28 (73 assertions) reviewer-executed; negative gates fail visibly | GLM round-3 delta: 0 critical/high/medium; low-1 rollback-failure fake divergence, low-2 requireTestDatabase dead export, low-3 compaction Ollama default recorded |
| 1A — memory deletion (R01) | ACCEPTED | `src/memory/**`, `src/tool/builtin/memory.ts`, ingest/archivist maintenance callers; fix extension: diary test caller line + package.json script | Phase 0 | `phase1a-memory-auth`, `phase1a-fix-race-proof` / `codex/gpt-5.6-luna(high)` | 31/31 (79 assertions) reproduced twice; real-DB race case gated + wired into `test:integration-required`; tsc clean in lease | GLM review: conformant, 0 critical/high; GLM fix delta: PASS. Notes for Phase 6: stricter audit-count assertion, interleaving nuance, moveBlock/updateBlockTier owner-check gap, real-DB execution pending endpoint |
| 1B — runtime bounds (R02/R03) | ACCEPTED | `src/runtime/**`, `src/tool/{registry,types}.ts` execution-options threading, `src/config/schema.ts` byte limits | Phase 0; serialized before 1C | `phase1b-runtime-bounds`, `phase1b-runtime-tests`, `phase1b-fix-lows` / `codex/gpt-5.6-luna(high)` | 86/86 tests (265 assertions); six named AC.3/AC.4 scenarios verified fail-on-old; deno check exit 0 | GLM: 0 critical/high; lows fixed (synchronous aborted-signal cancel, debug-frame isolation, dead lifecycle.signal removed, stdout_overflow reason) |
| 1C — custom tools/registry (R06) | ACCEPTED (fix delta review PASS; all 4 findings closed) | `src/custom-tool/**`, `src/tool/{registry,types}.ts`, custom tool builtin; fix extension: executor.ts secret filtering + secrets.ts validation | Phase 0 contracts/transactions | `phase1c-custom-tools`, `phase1c-tests-followup`, `phase1c-quarantine-fix`, `phase1c-review-fix` / `codex/gpt-5.6-luna(high)` | 119/119 focused (reviewer-reexecuted); quarantine reproducer 6/6; decode-tolerance fixture on real store; parse matrix runs real Deno; DB-gated AC.21 file wired | GLM review: conformant, 0 critical/high, 2M+2L → fix round → GLM delta PASS (INFO-A: record exact test-file lists in future ledger entries; custom-tool/postgres-store.test.ts still a harness-migration remainder) |
| 2D — history/projection | ACCEPTED (fix delta review PASS, 0 findings) | persistence history/compaction store + message/checkpoint stores + conversations domain + migrations 016/017 | Phase 0 contracts | `phase2d-history-projection`, `phase2d-tests-followup`, `phase2d-review-fix` / `codex/gpt-5.6-luna(high)` | 9/9 real-store fake suite (27); 26/26 phase0 (69); build clean; deterministic snapshot barrier test; DB-backed integration structured + gated | GLM review: conditional (1 high + 4 medium + 5 low) → fix round → GLM delta PASS with 7 info notes (seam divergences, M3 durable pointer → Phase 6 docs) |
| 2E — model/provider budgets | ACCEPTED (consolidated delta review PASS; 4 info notes) | `src/model/**`, `src/rate-limit/**`, `src/config/schema.ts` (exclusive window fields), additive src/testing transports | Phase 0 contracts | `phase2e-provider-budgets`, `phase2e-tests-followup`, `phase2e-usage-fix`, `phase2e-review-fix`, `phase2e-cancellation-tests`, `phase2e-cancellation-fix` / `codex/gpt-5.6-luna(high)` | 18/18 lifetime matrix (84 assertions); 359/359 model suite (850); 24/24 named E suites; build tree-wide clean; stop-and-report caught 2 real defect rounds (terminal-usage drop; deadline/SDK-options/stream-rejection defects) | GLM full review: substance conforms; M1/M2 → fixed → GLM delta PASS. Info carried: Ollama complete-path abort symmetry, deadline-source consistency nit, L1 fingerprints doc → Phase 6, L4 stream mutex caveat → Phase 3/5 |
| 3 — agent/checkpoint/root | ACCEPTED (consolidated delta PASS + micro-fix round spot-verified) | `src/agent/**`, `src/index.ts`, `src/composition-seam.ts`, history-store `restoreExactHistory` extension, builtin compaction-intent handler | 2D+2E accepted | `phase3-agent-lifecycle` + rounds 2–5b, `phase3-review-fix`, `phase3-review-fix2`, `phase3-review-fix3` / `codex/gpt-5.6-luna(high)` | slice4 9/9; fix-round 7/7; consolidated 4/4; 25/25 delta re-exec; 141-pass matrix; stop-and-report surfaced 3 real defect rounds (terminal-usage analog: auto-resume misroute, embedding drop, stub scenarios) | GLM consolidated: 3H/6M → fix rounds → GLM delta PASS w/ 1M+3L → micro-round spot-verified. F9 provenance production deferred to Phase 4 (recorded). DB-backed AC.19/AC.21 execution still endpoint-blocked |
| 4 — compaction atomicity | ACCEPTED (delta: code verified; evidence round + final consolidated round closed all findings) | `src/compaction/**`, agent provenance consumption, index/seam wiring, cooldown knob, enumeration boundary | 2D/2E/3 | `phase4-compaction`, `phase4-round2`, `phase4-review-fix`, `phase4-evidence-tests`, `phase-final-fix`, `phase-final-micro` / `codex/gpt-5.6-luna(high)` | 100 pass/29 intentional skips/218+ assertions; two-cycle continuity, provenance round-trip, wiring fit gate, recursive matrices, breaker matrix; destructive legacy path physically removed | GLM delta: production code verified, 5 evidence gaps → closed → final delta verified all 14 consolidated fixes clean; residuals closed by micro-round |
| 5 — MCP hardening | ACCEPTED (consolidated delta PASS + 2 micro-fix rounds spot-verified) | `src/mcp/**` (incl. discovery-bounds), custom-tool validator narrow extensions, index.ts MCP section, index.wiring tests | 2E/registry contracts | `phase5-mcp`, `phase5-index-wiring`, `phase5-review-fix`, `phase5-m3-fix`, `phase5-review-fix2` / `codex/gpt-5.6-luna(high)` | mcp 84/84 + wiring 63/63 + focused 245/245 + 189/189 post-micro-fix; real SDK 1.29.0 loopback forwarding proof; defaults asserted; stop-and-report caught H1/H2 schema-fidelity defects | GLM review: infrastructure PASS but 2H schema defects → fix rounds → GLM delta PASS (M-1 enum detail + L-1 four keywords closed by micro-round). L3 CLAUDE.md staleness → Phase 6 docs. M1 production wiring verified in delta |
| 6 — integrated validation/docs | ACCEPTED (validation sweep + docs lane + both final reviews + consolidated final fix + closing micro-round) | docs/**, config.toml.example, AGENTS.md guidance, E2E integration files, package.json wiring, final fix rounds | all implementation | `phase6-validation`, `phase6-docs`, `glm-final-adversarial-review`, `glm-final-persistence-review`, `phase-final-fix`, `glm-final-delta`, `phase-final-micro` | E2E seam scenario (env-gated + fake equivalent); command sweep: build PASS, deno check PASS, unit 28/28, ~60 named AC tests present, all safe suites green; AC.1–AC.21 audit in ledger; adversarial + persistence reviews → 2H+shared-M findings → consolidated fix verified clean by GLM final delta; closing micro-round closed last residuals (22/22 + 45/45) | ACCEPTANCE-CLEAN on all runnable evidence; remaining evidence is exclusively PostgreSQL-endpoint-blocked (documented in ledger + operator guide) |

## Completion statement (2026-09-09)

All phases 0–6 ACCEPTED on `remediation/integrity-lifecycle-20260908` (local only; no push/PR/merge/deploy). Every R01–R17 finding plus MCP/provider hardening is implemented with named regression evidence; both mandated final reviews (adversarial full-diff + independent persistence/compaction) ran on the GLM pin, and their findings were fixed and delta-verified clean. Remaining verification is exclusively environmental: every PostgreSQL integration suite is structurally real, wired, and prerequisite-gated, awaiting a safe disposable `TEST_DATABASE_ADMIN_URL`; Ollama embedding tests need a local endpoint. Deferred low-severity items are documented in the ledger disposition entries. The durable plan file records the operator overrides (direct-parent orchestration; GLM review pin) honored throughout.

## Worker result contract

Each worker must report: changed paths; public/durable contract changes; exact named tests and commands run with results; skipped prerequisites; remaining risks; and files leased. Workers may not stage, commit, reset, clean, push, deploy, or modify another lease. Any overlapping path requires parent serialization and reconciliation. **The execution ledger is append-only: modify it exclusively with anchored `file_edit_search_replace` at the end of the Event log — never `file_write` the whole file.**

## Required reviews

- Historical preflight: Astra (before override).
- Current rule (2026-09-09 operator override): every per-package and final independent review uses `zai/glm-5.3-flash(high)`.
- Final: full-diff/new-file adversarial review plus an independent persistence/compaction review; repeat every critical/high finding until resolved or explicitly rebutted.

## Event log

- 2026-09-08: operator moved orchestration from `workflow-lead` to parent. Previous delegate was cancelled after recovering the plan and environment only; no implementation worker remained active. *(reconstructed)*
- 2026-09-08: created `remediation/integrity-lifecycle-20260908` from the existing dirty working tree. *(reconstructed)*
- 2026-09-08: Astra preflight review reported no critical findings, one high ownership conflict, two medium transaction/acceptance ambiguities. Durable plan amended (direct-parent override recorded; outermost-transaction-only durability/reconciliation; AC.10 third fail-closed outcome). Repeat Astra preflight passed with no findings before implementation dispatch. *(reconstructed)*
- 2026-09-08: Phase 0 initial implementation (`phase0-foundation`): test doubles, isolated DB harness, contracts, composition seam, provider-scoped transactions, migration 015. Build + 14 unit tests passed; integration gate failed visibly without env var. **Astra review rejected:** URL query override/redaction bypass, unknown-COMMIT reconciliation gaps, COMMIT tag ignored, non-representative fakes, legacy suite not migrated, inert seam, publication/migration-boundary gaps. *(reconstructed)*
- 2026-09-09: Phase 0 repair round 1 (`phase0-remediate-review`): query-option rejection + field-wise pg config, reconciled commit outcomes with rollback tag validation, transactional in-memory persistence, production agent/subconscious/archivist construction via seam, bounded transports, `tx-nesting` migrated off shared DB, updated scripts. Build pass; `test:unit` 20/20; gate visible. Real PostgreSQL evidence still blocked. *(reconstructed)*
- 2026-09-09: operator directed reviews use `zai/glm-5.3-flash(high)`; in-flight Astra Phase 0 re-review cancelled and relaunched on GLM. *(reconstructed)*
- 2026-09-09: GLM Phase 0 round-2 review: H1/H2 resolved; residuals = fake/adapter reconciler-throw divergence (medium), plain `bun test` ungated exposure incl. archivist operational-DB fallback (medium), low items. Round-3 repair (`phase0-remediate-round3`) fixed both mediums + lows; GLM round-3 delta review: 0 critical/high/medium, 3 low + 4 info documented → **Phase 0 accepted**. DB-backed AC.21 execution remains blocked pending `TEST_DATABASE_ADMIN_URL`. *(reconstructed)*
- 2026-09-08/09: Phase 1A implemented (`phase1a-memory-auth`): pure deletion policy, FOR UPDATE recheck + atomic audit+delete, narrow maintenance ports, replaceWorkingMemory capability (unwired), ingest/archivist callers converted. GLM review: conformant, 0 critical/high, M1 = missing real-concurrency proof + AC.2 file unwired. Fix round (`phase1a-fix-race-proof`): real two-connection FOR UPDATE serialization case, script wiring, compile-time-required owner, MUTATION_REJECTED codes, diary caller fix, barrel/port cleanups. GLM fix delta review: **PASS** → **1A accepted**. Notes carried: stricter audit-count assertion, interleaving nuance, moveBlock/updateBlockTier owner-check gap (later package), real-DB execution pending endpoint.
- 2026-09-09: Phase 1B implemented (`phase1b-runtime-bounds`): single-owner OPEN→CLOSING→CLOSED lifecycle, raw byte budgets before decode, byte-first Deno bridge parsing, ExecutionOptions threading, config byte limits with exact plan defaults. Worker honestly disclosed missing named tests → follow-up (`phase1b-runtime-tests`) added all six named AC.3/AC.4 scenarios (6/6, 51 assertions) + RuntimeProcess seam. GLM review: all 10 requirements pass, 0 critical/high; lows = pre-aborted signal mislabeled, `__debug__` flipping success, dead lifecycle.signal, stdout overflow reason label. Fix round (`phase1b-fix-lows`) resolved all + 2 new regressions; 86/86 (265 assertions), build green → **1B accepted**.
- 2026-09-09: Phase 1C implemented (`phase1c-custom-tools`): strict metadata validation (no String/Boolean coercion), registry reservation/atomic-replace/quarantine primitives, serialized manager mutations, receipt insertion + independent reconciliation via withTransactionOutcome, loadAll quarantine, builtin coercion removed. Targeted suites 64/64 (164 assertions); `custom_metadata_rejection_matrix` present. **INCIDENT:** worker overwrote this ledger (13 lines remained); parent restored from session records. Worker also failed to implement 4 of 5 required named tests (`registry_reservation_commit_failure`, `invalid_persisted_tool_quarantine`, `generated_script_parse_matrix`, `custom_tool_commit_ack_reconciliation`); follow-up `phase1c-tests-followup` dispatched. No operational DB, migration, push, commit, or live API was used at any point in this log.
- 2026-09-09: Phase 2D (`phase2d-history-projection`) and 2E (`phase2e-provider-budgets`) dispatched on disjoint leases; 2D holds the exclusive sequential-migration lease (recheck-before-allocate rule), 2E holds exclusive `src/config/schema.ts`.
- 2026-09-09: Phase 1C test follow-up (`phase1c-tests-followup`): added `registry_reservation_commit_failure` reproducer at `src/custom-tool/registry-reservation-commit-failure.test.ts`. Rollback and reconciled-commit cases pass, but lost-ack and reconciliation-unavailable cases fail because `CustomToolManager.runMutation` releases the registry reservation without quarantining the affected name; `registry.getQuarantines()` remains empty after `custom tool mutation outcome unknown`. Per lease, implementation files were not changed and remaining Package C suites were not added. `git diff --check` passed; no database, migration, push, commit, or live API used. Ledger update was append-only via anchored edit.
- 2026-09-09: Package 2E (`phase2e-provider-budgets`, general-purpose/codex-gpt-5.6-luna) implemented pure exchange grouping/shaping, serialized request budgeting with safety margin and summarizer-window diagnostics, usage subset normalization, caller signal/absolute deadline contracts, typed cancellation, deadline-aware retry backoff, OpenRouter default-on and generic OpenAI opt-in stream usage flags, cumulative terminal stream usage (including empty-choice usage chunks), Anthropic cache-policy stability, and Ollama lifetime/terminal usage handling. Changed paths: `src/model/{types,exchange,budget,usage,cancellation,retry,anthropic,openai-shared,openai-compat,openrouter,ollama,index}.ts`, `src/model/{protocol,cancellation,config-window}.test.ts`, `src/rate-limit/provider.ts`, `src/config/schema.ts`, `src/errors/model.ts`. Evidence: initial and independent provider/model/rate-limit/phase0 targeted commands passed 238/238 tests (551 assertions); final focused provider/model/rate-limit/phase0 command passed 181/181 (434 assertions); final `bun test src/testing/phase0.test.ts` passed 20/20 (55 assertions); `git diff --check` passed. A subsequent repository-wide `bun run build` is blocked only by unrelated active custom-tool test errors at `src/custom-tool/registry-reservation-commit-failure.test.ts:61,67`; no in-scope model/rate-limit diagnostics were reported. No live provider, database, migration, stage, commit, push, or deployment performed. Remaining integration risk: agent/compaction admission and recursive summary wiring are later leases; rate-limit streaming remains delegated rather than fully usage-reconciled.
- 2026-09-09: Package 2D (`phase2d-history-projection`) implemented retained transcript + revisioned active history: migration `016_conversation_history.sql` allocated after rechecking 015 as on-disk max (conversation revision state, ordered active membership with composite referential integrity, deterministic repeat-safe backfill, provenance/archive references, receipt linkage, DB-level immutability for history-owned memory blocks; canonical rows retained in existing `messages` table); `src/persistence/conversation-history-store.ts` (transactional append, consistent active reads + revision, strict retained-ID reads, explicit historical reads, compaction-source enumeration, prepared commit, idempotent receipts, fail-closed `history_state_unknown`); message-store count/list/latest moved to active projection; checkpoint-store `saveAndPruneCheckpoint()` atomic save/prune; search conversations domain default→active, explicit historical→retained/superseded with filters preserved; index exports. Evidence: build passed, phase0 suites green (20/20, 6/6), git diff --check pass; DB-backed suites fail visibly without env var. NAMED TESTS NOT IMPLEMENTED in this pass → `phase2d-tests-followup` dispatched (history_projection_migration_preserves_rows, compaction_stale_revision, compaction_write_failure_reload_matrix, compaction_commit_ack_reconciliation, checkpoint_prune_retains_transcript, readActive snapshot consistency, foreign_message_active_prevention, retained_archive_rejects_public_mutation). Worker's own ledger append did not land; parent recorded this entry. Cross-package handoffs recorded: Phase 3 must migrate agent/root appends + checkpoint-restore reads to ConversationHistoryStore; Phase 4 must consume enumerateCompactionSources()/commitCompaction(); memory/ingest/archivist owners must route history-owned archive mutations through the dedicated contract.
- 2026-09-09: Package E test follow-up stopped on an in-scope provider defect. Added reproducer `src/model/provider-terminal-usage-matrix.test.ts` with required suite name `provider_terminal_usage_matrix`; loopback Anthropic SSE sends `message_delta.usage` (`output_tokens: 7`) after `message_start` (`input_tokens: 3`), but adapter terminal `message_stop.message.usage` is `undefined` instead of cumulative usage. Command `bun test src/model/provider-terminal-usage-matrix.test.ts`: **1 failed, 0 passed**. Per lease, no implementation files were modified and remaining named suites were not added. No database, migration, live API, commit, push, or deployment used.
- 2026-09-09: Package E targeted remediation: `src/model/anthropic.ts` now retains normalized `message_start` usage and maps `message_delta` to one cumulative terminal usage record (Anthropic input/cache subsets from start plus final output), while omitting usage when both provider events omit it. Audit found no equivalent terminal-usage loss in `openai-compat.ts`, `openrouter.ts`, or `ollama.ts`; OpenAI-family usage-only chunks are processed before empty-choice skips and Ollama retains its final cumulative counts. Added and ran the five leased named suites: `provider-terminal-usage-matrix.test.ts`, `provider-wire-contract-matrix.test.ts`, `provider-lifetime-cancellation-matrix.test.ts`, `usage-accounting-no-double-count.test.ts`, and `exchange-shaping-protocol-matrix.test.ts`. Remaining verification and exact outcomes are recorded in the worker handoff.
- 2026-09-09: Package C targeted-fix follow-up (`phase1c-quarantine-fix`): `src/custom-tool/manager.ts` now quarantines the affected name on `commit_unknown` before releasing its reservation, blocks ordinary re-registration over quarantined names, and permits only `loadAll()` trusted recovery to clear quarantine before validated publication; reconciled commits publish and confirmed rollbacks preserve prior callable definitions. `src/tool/{registry,types}.ts` gained the trusted reservation contract, direct registration quarantine guard, and generated `array` alias for parseable Deno stubs while preserving the existing signature. Added `src/custom-tool/{invalid_persisted_tool_quarantine.test.ts,generated_script_parse_matrix.test.ts,custom_tool_commit_ack_reconciliation.integration-required.test.ts}` and completed the existing `registry-reservation-commit-failure` reproducer. Evidence: `bun test src/custom-tool/registry-reservation-commit-failure.test.ts src/custom-tool/invalid_persisted_tool_quarantine.test.ts src/custom-tool/generated_script_parse_matrix.test.ts` **8/8, 31 assertions**; existing `bun test src/custom-tool/manager.test.ts src/custom-tool/validation.test.ts src/tool/registry.test.ts src/tool/builtin/custom-tools.test.ts` **64/64, 164 assertions**; `bun run build` and `git diff --check` pass. The fake create/update/delete outcome supplement passes. The required integration file fails visibly without `TEST_DATABASE_ADMIN_URL` (**1 pass, 1 expected prerequisite failure**), and its PostgreSQL describe block is not registered without that variable; no real PostgreSQL execution, migration, push, commit, or deployment was claimed or performed. Remaining review disposition: pending parent review; Package C integration evidence remains blocked on an explicit disposable endpoint.
- 2026-09-09: Package 2D test follow-up (`phase2d-tests-followup`) added fake-level named regressions in `src/persistence/conversation-history.test.ts` (stale revision, six-stage rollback/unknown matrix, idempotent receipt reconciliation, foreign-message prevention, retained archive mutation rejection, checkpoint pruning independent of transcript) and required PostgreSQL scenarios in `src/persistence/conversation-history.integration-required.test.ts` (pre-016 deterministic/repeat-safe migration backfill with byte-identical messages, six-stage before/after fault injection, lost-ack reconciliation plus fail-closed unavailable reconciliation, pruning retention, concurrent-provider snapshot checks, composite FK, archive trigger immutability, historical search labels). Added minimal `if (database)` teardown guards to `src/persistence/message-store.test.ts`, `src/persistence/checkpoint-store.test.ts`, and `src/search/domains/conversations.test.ts`. Verification: fake suite 6/6 (71 assertions), `bun run build` pass, `src/testing/phase0.test.ts` 20/20 (55 assertions), `git diff --check` pass; required integration suite and migrated persistence/search suites fail visibly at missing `TEST_DATABASE_ADMIN_URL` as intended, without teardown crashes. No implementation defect reproduced; no database was created. Parent should add `src/persistence/conversation-history.integration-required.test.ts` to `test:integration-required` and retain the fake suite in targeted/unit coverage; no package.json edits were made under lease.
- 2026-09-09: Package E targeted-fix and test-completion round: changed `src/model/anthropic.ts` only in production; retained normalized `message_start` usage and merged it with `message_delta` output/cache semantics into one terminal cumulative `message_stop` usage, preserving omission when normalized usage is absent. Audited `src/model/openai-compat.ts`, `src/model/openrouter.ts`, `src/model/openai-shared.ts`, and `src/model/ollama.ts`; no same-class terminal usage loss or double-counting defect found. Added leased suites `src/model/provider-wire-contract-matrix.test.ts`, `src/model/provider-lifetime-cancellation-matrix.test.ts`, `src/model/usage-accounting-no-double-count.test.ts`, and `src/model/exchange-shaping-protocol-matrix.test.ts`, and extended `src/model/provider-terminal-usage-matrix.test.ts` across all four provider families. Final named command passed **24/24 tests, 51 assertions**: `bun test src/model/provider-terminal-usage-matrix.test.ts src/model/provider-wire-contract-matrix.test.ts src/model/provider-lifetime-cancellation-matrix.test.ts src/model/usage-accounting-no-double-count.test.ts src/model/exchange-shaping-protocol-matrix.test.ts`; `git diff --check` passed. Expanded current model/config/rate-limit/Phase 0 baseline passed **338/338 tests, 765 assertions** across 16 files. `bun run build` passed (`tsc --noEmit`). Design questions reported only: OpenAI/Anthropic still lack a shared composed absolute-deadline retry context (Ollama has one), so cancellation/deadline behavior remains adapter-inconsistent and should be unified in a later change; `ModelResponse` retains zero-fallback usage for compatibility, while stream events omit usage when provider usage is absent, so callers must not infer zero usage from missing stream fields. No database, live API, migration, stage, commit, push, or deployment used; integration-required PostgreSQL evidence remains blocked on explicit `TEST_DATABASE_ADMIN_URL`.
- 2026-09-09: Package E Phase 2 review-fix round (`phase2e-cancellation-integrity-fix`, GLM findings M1/M2 plus L3-iv/L2 follow-up): `src/model/{anthropic,openai-compat,openrouter}.ts` now compose caller signal, absolute deadline, and timeout upper bound for every complete/stream lifetime; composed signals are passed to SDK request options and `callWithRetry`, stream generators dispose listeners/timers on all exits, and Anthropic/OpenAI-family user-abort errors classify as typed non-retryable `CANCELLED` unless the composed timeout/deadline expired (`TIMEOUT`). `src/rate-limit/provider.ts` now makes mutex queue waits and bucket-refill acquisition abortable, honors the same deadline, releases canceled queue continuations, and cleans abort listeners; `src/rate-limit/provider.test.ts` adds and passes abort-mid-acquisition, queued-deadline, mutex-cancellation, and normal-acquisition cases. `src/model/openai-shared.ts` provides the OpenAI-family abort predicate; OpenAI-compatible `message_start` omits usage when absent rather than fabricating zero, matching other families. Verification: `bun run build` passed (`tsc --noEmit`); final named Package E/adapter/rate-limit/Phase 0 command passed **201/201 tests, 480 assertions** across 12 files; `git diff --check` passed. LSP diagnostics did not publish within their timeout, with no compiler diagnostics observed. L1 fingerprints-caveat documentation remains Phase 6; L4 `syncFromServer`/stream mutex caveat remains noted for Phase 3/5 wiring; design question (b) confirmed acceptable residual. No database, live API, migration, stage, commit, push, or deployment used; PostgreSQL integration evidence remains blocked on explicit `TEST_DATABASE_ADMIN_URL`.
- 2026-09-09: Package E cancellation hardening follow-up: added stream-consumption cancellation mapping in Anthropic/OpenAI-compatible/OpenRouter generators so aborts after SDK stream creation are typed `CANCELLED`/`TIMEOUT` while non-cancellation stream errors remain unchanged; disposal remains unconditional. Verification: `bun run build` and adapter/lifetime/rate-limit command passed **103/103 tests, 260 assertions**; `git diff --check` passed. No database, live API, migration, stage, commit, push, or deployment used.
- 2026-09-09: Package D review fix round (`phase2d-fix-round`, GLM conditional-acceptance findings): H1 fixed by rewriting `src/persistence/conversation-history.test.ts` so all named fake-level regressions use the real `createConversationHistoryStore(createInMemoryPersistence())`, including append, stale revision, rollback/unknown fail-closed, idempotent receipt replay, strict `readByIds`, historical labels, snapshot behavior, and `saveAndPruneCheckpoint` transcript retention; the handwritten fake implementation was removed. M1/M2 fixed: `readActive` creates the empty state row and locks it with `FOR SHARE` before reading membership; the required PostgreSQL scenario now uses deferred barriers, proves append is blocked between the reader's state and membership statements, then asserts one coherent snapshot (no race/sleep correctness). M3 deferred and recorded: no leased-path metadata schema/operation recognition is unambiguous enough to backfill legacy archive ownership safely; Phase 4 must define and verify exact operation-qualified provenance/batch recognition before changing ambiguous rows, which remain untouched. M4 fixed by making `src/contracts/history.ts` a compatibility barrel over authoritative persistence-store types while retaining legacy aliases required by `src/contracts/index.ts`; divergent unimplemented shapes are eliminated. L1 fixed by implementing `history: 'all'` as an active-plus-retained projection with `active`/`superseded` labels and forwarding history through `createSearchStore`; `readHistorical` labels active retained rows `historical` and non-members `superseded`. L3/L2/L4 fixed with real-store strict-membership coverage, stable `history_membership_mismatch`, `history_stale_revision`, and `history_stale_membership` codes. L5 fixed with migration `017_scope_history_summary_fk.sql`, a guarded composite FK from `(conversation_id, summary_message_id)` to `(conversation_id,id)`; 016 was rechecked and not edited. The in-memory persistence seam was extended only to model the real leased SQL behavior and preserve Phase 0 outcomes. Verification: `bun test src/persistence/conversation-history.test.ts` **9/9, 27 assertions**; `bun test src/persistence/phase0-transaction.test.ts src/testing/phase0.test.ts` **26/26, 69 assertions**; `bun run build` **pass**; `git diff --check` **pass**. `env -u TEST_DATABASE_ADMIN_URL bun test src/persistence/conversation-history.integration-required.test.ts` fails visibly at the required prerequisite (`integration database required: set TEST_DATABASE_ADMIN_URL`), with no database created. No package.json edits, operational database, live API, commit, push, or deployment performed.
- 2026-09-09: Phase 1 Package C targeted remediation (`phase1c-quarantine-fix`, follow-up GLM findings): MEDIUM-1 closed by making custom-tool PostgreSQL row decoding tolerant and reporting per-row decode issues; `loadAll()` continues valid rows, quarantines corrupt names with bounded visible reasons/counts, and never rewrites malformed storage. MEDIUM-2 closed by sharing reserved runtime-binding rules across metadata validation, secret injection, and `secret_set`; colliding names (`PARAMS`, `output`, `debug`, `__callTool__`, `console`, `Deno`, `globalThis`, and reserved words) are filtered/rejected. LOW-3 closed: provisional nested-transaction outcomes are fail-closed and quarantined, never published. LOW-4 closed: post-commit publication exceptions quarantine the affected name and surface the original error, leaving the state explicit as committed in storage/quarantined in the registry. Changed leased paths: `src/custom-tool/{manager,postgres-store,types,validation}.ts`, `src/custom-tool/{postgres-store-tolerance,invalid_persisted_tool_quarantine,generated_script_parse_matrix,registry-reservation-commit-failure}.test.ts`, `src/runtime/{executor,executor-secrets.test}.ts`, `src/tool/{registry,types}.ts`, `src/tool/builtin/{custom-tools,secrets}.ts`. Verification: focused Package C command across 10 files passed **116/116 tests, 327 assertions**; 1B executor/secrets baseline passed **59/59**; `bun test src/testing/phase0.test.ts` passed **20/20**; `git diff --check` passed. `bun run build` currently fails only on concurrent off-lease `src/model/openai-compat.ts` unused imports and duplicate `OpenAI` imports in `src/model/openai-shared.ts`; no Package C diagnostics were reported and no off-lease files were changed. The integration-required PostgreSQL custom-tool suite remains visibly blocked without `TEST_DATABASE_ADMIN_URL` (one fake test passes and the prerequisite test fails as designed); no operational database, migration, live API, commit, push, or deployment was used. GLM review disposition: original Package C review had **0 critical/high, 2 medium, 2 low**; all four are now addressed by implementation and regression coverage. INFO carry-forward for Phase 6 CLAUDE.md/docs (no code changes in this round): INFO-5 `unregister()` clears quarantine records; safe with current callers but future unregister callers could clear unknown-outcome quarantine. INFO-6 dispatch requires an object payload before schema evaluation, so a non-object root schema is load-valid but permanently uncallable; it fails closed. INFO-7 non-string parameter types with string `enum_values` are accepted at create but every dispatch value fails strict enum comparison; document or add compatibility validation later. INFO-8 `validateSchemaValue` is a subset JSON-Schema validator without additionalProperties/pattern/min-max/numeric-bound enforcement; unsupported constraints are unchecked rather than coerced. INFO-9 bounded quarantine/validation reasons echo hostile schema property keys as logged text only. INFO-10 `inputSchema` cannot currently be removed through manager merge semantics, and direct store updates with only inputSchema can reset parameters to `[]`; manager production calls always pass parameters. INFO-11 the parse-matrix title “escaped enums” overstates coverage because descriptions/enums are structurally absent from generated source; JSON escaping remains asserted indirectly for PARAMS/secrets. Real PostgreSQL AC.21 receipt/reconciliation execution remains unverified pending a disposable endpoint.
- 2026-09-09: Package E cancellation test-completion round stopped on in-scope provider defects. Added leased `src/model/provider-lifetime-cancellation-matrix.test.ts` with exact tests: `composes signal and deadline and classifies deliberate cancellation separately`; `classifies an expired absolute deadline as timeout`; generated per-family `anthropic|openai-compat|openrouter|ollama cuts a mid-stream request at its absolute deadline`; generated per-family `anthropic|openai-compat|openrouter|ollama cancels a deadline-only caller with no timeout configured`; `anthropic maps deliberate mid-stream AbortSignal to non-retryable CANCELLED`; `openai-compat maps deliberate mid-stream AbortSignal to non-retryable CANCELLED`; `retries transient failures and succeeds within one outer deadline`; `classifies a deadline expiring during retry backoff as TIMEOUT, not CANCELLED`; `classifies a caller abort during retry backoff as CANCELLED`. Added leased `src/rate-limit/provider-lifetime.test.ts` with `fails a deadline-expired rate-limit acquisition before provider invocation`, `releases a canceled mutex waiter so a later caller proceeds`, and `preserves typed provider cancellation errors through the rate-limit wrapper`. Targeted command `bun test src/model/provider-lifetime-cancellation-matrix.test.ts src/rate-limit/provider-lifetime.test.ts`: **8 passed, 10 failed, 2 unhandled errors, 33 assertions**. All 3 focused rate-limit tests passed. Reproducer: Anthropic/OpenAI-family adapters pass `timeout: undefined` to the installed SDK when signal/deadline is supplied, producing `timeout must be an integer` before loopback invocation; OpenAI-family stream failures also surfaced typed `TIMEOUT` while the test process reported an unhandled rejection; Ollama stream deadline surfaced raw `DOMException TimeoutError` rather than typed `ModelError`. Per lease, no implementation files were modified; full suite, Phase 0, and build were not run after the in-scope defect. `git diff --check` passed. Existing unrelated dirty paths, including `package.json` and prior implementation changes, were preserved; no database, live API, migration, stage, commit, push, or deployment used.
- 2026-09-09: Phase 2 Package E correction round (`phase2e-cancellation-fix`): fixed the three defects reproduced by the cancellation-test round — (1) `timeout: undefined` passed to Anthropic/OpenAI-family SDKs on deadline-only calls (`buildCancellationRequestOptions`: omit timeout when unbounded, integer-ceiling remaining ms when bounded); (2) orphaned stream rejections in OpenAI-family generators (streams retained + aborted in `finally`; suppressed abort completion surfaced as typed TIMEOUT/CANCELLED); (3) Ollama raw `DOMException` mapped to retryable TIMEOUT / non-retryable CANCELLED with body cancellation. Evidence: 18/18 matrix+rate-limit (84 assertions); 359/359 model/config/rate-limit (850 assertions, 21 files); phase0 20/20; `git diff --check` clean. Parent fixed the residual TS6133 unused-param in the matrix test fixture and re-executed `bun run build` → **clean pass tree-wide**. Correction-round results were initially mis-appended to the session log; parent recorded this entry.
- 2026-09-09: Phase 3 continuation round-1 (`integrity-lifecycle-20260908`): implemented bounded FIFO ingress serialization, cancellation-aware admission/slot release, production history-store append/read seam, typed tool-outcome persistence and legacy `outcome_unknown` decoding, and additive checkpoint fields. Verification: build passed; focused agent/checkpoint/history run passed 63/66, with the three checkpoint failures attributable to preserving accepted v1 compatibility; DB-gated checkpoint restore remained blocked by missing `TEST_DATABASE_ADMIN_URL`. Remaining Phase 3 work was explicitly not claimed: durable unfinished-batch recovery, deferred compaction/actual request admission, real checkpoint v2 migration/triggers, startup restore selection, and seam routing tests.
- 2026-09-09: Phase 3 continuation round-2: re-inspected the leased contracts and confirmed the above bounded subset remains present; no additional B–F implementation could be safely completed within this continuation. Required integration target remains `src/agent/phase3.integration-required.test.ts`; build/diff verification and exact named-suite results are to be run/reported by the parent after any further implementation. No persistence implementations, config, migrations, database, live API, commit, push, or deployment were used.
- 2026-09-09: Phase 3 continuation round-2 results (`integrity-lifecycle-20260908`): completed checkpoint v2 as a real versioned codec (`CHECKPOINT_VERSION = 2`), required v2 transcript revision/archive/provenance fields, explicit v1→v2 migration with `migratedFromVersion: 1` provenance-gap marker, strict unknown-future rejection, and v2 missing-retained-message rejection before restore mutation; removed restore-only legacy working-memory count/content/identifier limits; forwarded provenance through checkpoint creation/agent state; added safe `src/agent/phase3.integration-required.test.ts` with named restart/restore scenarios and no-default-database gating. Verification: `bun run build` pass; agent/checkpoint suites **64/64**; outcome/history/Phase 0 plus Phase 3 gate **32 pass, 3 safe skips**; `git diff --check` pass. Remaining Phase 3 B/C/E named lifecycle work is not claimed: durable unfinished-batch terminal outcomes/recovery latch, deferred compaction and actual assembled-request admission, completed-boundary checkpoint triggers, startup auto-resume/explicit restore transaction wiring, and full production seam routing/restart integration execution. No operational database, live API, migration, commit, push, or deployment used.
- 2026-09-09: Phase 3 slice-3 B/C implementation (`integrity-lifecycle-20260908`): added `src/agent/integrity-lifecycle.ts` with durable `agent_batch` receipts, startup recovery detection, trusted all-call backfill, coalesced `compaction_intent` receipts, and runtime validation of persisted outcomes; `src/agent/agent.ts` now checks recovery before admission, serializes tool batches with begin/record/complete, backfills unstarted calls as `cancelled` and started-but-uncertain calls as `outcome_unknown`, latches recovery-required on persistence failure, defers `compact_context` acceptance to completed-batch admission, evaluates assembled requests with reserve/margin and exchange-group hard guard, and reports non-blocking compaction failures through traces; `src/index.ts` injects lifecycle for the production main agent and `src/agent/index.ts` exports the seam. Updated deferred-compaction expectations and added/retained named restart gate in `src/agent/phase3.integration-required.test.ts`. Verification: `bun run build` pass; Phase-3 focused command across agent/context/snapshot/phase3-gate/model-budget/exchange/tool suites **83 pass, 3 safe PostgreSQL skips, 0 fail** (220 assertions); checkpoint serializer/create/outcome/history-store unit baseline **33 pass, 0 fail** (109 assertions); `git diff --check` pass. The separate DB-backed `history-loading.test.ts`/`checkpoint-restore.test.ts` command was not executable without `TEST_DATABASE_ADMIN_URL` and failed at the existing prerequisite/teardown harness; no database was created. Remaining honest scope: required named fake-level lifecycle matrix tests (incomplete_batch_blocks_future_provider_calls, queue_failure_and_cancel_release, mixed_ingress_serializes_complete_turns, registry_failures_reach_next_model_request, tool_outcome_database_roundtrip, legacy_outcome_no_substring_inference, compaction intent/admission/cache publication cases) were not added; checkpoint boundary triggers/startup auto-resume wiring and full restart execution remain unverified; E-remainder/F remain parent scope. No operational database, live API, migration, commit, push, or deployment used.
- 2026-09-09: Phase 3 slice-4 (`integrity-lifecycle-20260908`) verification and completion: `src/agent/phase3-slice4.test.ts` uses real `createAgent`, real `createToolRegistry`, real `createConversationHistoryStore`, and transactional in-memory persistence. Named regressions cover `mixed_ingress_serializes_complete_turns`, `queue_failure_and_cancel_release`, `incomplete_batch_blocks_future_provider_calls`, `registry_failures_reach_next_model_request`, `tool_outcome_database_roundtrip_and_legacy_outcome_no_substring_inference`, compaction intent coalescing/admission and remeasurement, typed `context_unfittable` with zero provider calls, and cache publication. D-remainder implemented in `src/agent/agent.ts`: immutable checkpoint snapshots, completion-only interval counters, deferred explicit checkpoint requests at completed-batch boundaries, pre-compaction checkpoint capture against durable history revision, and serial `Agent.shutdown()` drain; `src/index.ts` passes the main agent drain to shutdown. Named checkpoint tests `agent_checkpoint_real_trigger_matrix`, `resume_counter_and_interval_continuity`, and `interval_save_failure_does_not_fail_turn` pass. Defects found/fixed: mutable/no-op checkpoint publication, failed-turn interval advancement, immediate explicit checkpoint handling, missing per-ingress cancellation, and missing production shutdown drain. Verification: slice-4 **9/9 tests, 26 assertions**; non-PostgreSQL agent/history/Phase-0 baseline **239/239 tests, 688 assertions**; `bun run build` pass; `git diff --check` pass; Phase 3 integration gate **1 prerequisite pass, 3 safe skips, 0 fail**. DB-backed history-loading/checkpoint-restore remain blocked before test bodies because `TEST_DATABASE_ADMIN_URL` is unset, with secondary legacy teardown errors; no database created. Remaining scope: **E-remainder + F only** (startup restore selection/full restart execution and final integration/operational wiring). No migration, live API, commit, push, or deployment used. required named fake-level lifecycle matrix tests (incomplete_batch_blocks_future_provider_calls, queue_failure_and_cancel_release, mixed_ingress_serializes_complete_turns, registry_failures_reach_next_model_request, tool_outcome_database_roundtrip, legacy_outcome_no_substring_inference, compaction intent/admission/cache publication cases) were not added; checkpoint boundary triggers/startup auto-resume wiring and full restart execution remain unverified; E-remainder/F remain parent scope. No operational database, live API, commit, push, or deployment used.
- 2026-09-09: Phase 3 slice-5 (`integrity-lifecycle-20260908`) exact restore and E/F wiring: additively extended `src/persistence/conversation-history-store.ts` with `restoreExactHistory(plan: PreparedExactRestorePlan)`, where the plan requires operationId, conversationId, expectedRevision (`number | null` only for revision-zero first restore), explicit ordered unique retained messageIds, checkpointId, sourceArchiveIds, and provenanceRefs. The operation owns an outermost `withTransactionOutcome` scope; it idempotently replays an existing committed receipt, locks/reads the current revision, validates every requested ID belongs to the conversation before deleting membership, replaces membership in explicit order, allocates `currentRevision + 1` without rewinding, writes `checkpoint_restore` receipt and `conversation_history_provenance`, then independently reconciles commit-unknown outcomes. It returns the committed `ExactRestoreReceipt` and `ActiveHistory`; confirmed rollback/reconciled rollback return the original failure, while unknown commit/publication returns `history_state_unknown` for caller reconciliation. `src/testing/ports.ts` models retained-ID validation, ordered membership replacement, receipt/provenance persistence, rollback, lost-ack commit, and unresolved reconciliation faults. `src/agent/checkpoint-restore.ts` now uses durable-first exact restore for supplied historyStore and calls `replaceWorkingMemory` only after durable success; v1 checkpoints report a provenance gap and v2 archive/provenance references are forwarded. `src/composition-seam.ts` and `src/index.ts` now refuse recovery-required startup, route explicit resume through the restore seam, and auto-resume only reads the durable active projection (preserving post-checkpoint commits; no checkpoint membership/memory replay). Added `src/agent/phase3-slice5.test.ts` and `src/composition-seam.phase3.test.ts`. Verification: `bun run build` pass; focused exact-restore/startup/history/Phase-0 command **25/25 tests, 82 assertions**; Phase-3 agent/checkpoint/outcome/Phase-0 baseline **62/62 tests, 190 assertions**; `bun test src/agent/phase3.integration-required.test.ts` **1 pass, 3 safe skips** because `TEST_DATABASE_ADMIN_URL` is unset; `git diff --check` pass. Changed paths in this round: `src/index.ts`, `src/agent/checkpoint-restore.ts` (prior durable branch), `src/persistence/conversation-history-store.ts`, `src/persistence/index.ts`, `src/testing/ports.ts`, `src/agent/phase3-slice5.test.ts`, `src/composition-seam.ts`, `src/composition-seam.phase3.test.ts`. No migration files, operational database, live API, commit, push, or deployment used.
- 2026-09-09: Phase 3 consolidated-review correction round (`integrity-lifecycle-20260908`): corrected prior ledger inaccuracies that overstated F6 cache publication and F15 ingress/deadline coverage as complete without the consolidated-review evidence; current source verifies cache/snapshot reset only after a successful committed compaction replacement, and model requests forward per-ingress signal/deadline. Confirmed fixes for F1 auto-resume null checkpoint routing, F2 history-path embedding generation and SQL persistence with null-on-failure, F4 typed execute_code failures, F7 typed reentrant ingress rejection, F8 durable completion-only turn counter, F10 atomic save-and-prune checkpointing, F11 one shutdown capture, F12 loop-detector warning persistence, F13 guarded completion reread, and F16 typed lifecycle recovery/outcome errors. Replaced `src/agent/phase3.integration-required.test.ts` stubs with real gated scenarios: unfinished receipt recovery blocks a fresh provider, fresh persistence restore verifies exact membership/revision/historical retention, and auto-resume reads durable active history while preserving post-checkpoint commits. Added `src/phase3-consolidated-review.test.ts` named regressions: `live_write_checkpoint_restore_compatibility`, `auto_resume_after_committed_compaction`, `remediation_end_to_end_restart_scenario`, and `checkpoint_version_and_missing_source_matrix` (4/4 tests, 14 assertions). Fixed `src/testing/ports.ts` receipt-details decoding and durable agent-batch/compaction-intent SQL modeling; direct restart probe confirms unresolved call IDs survive lifecycle recreation. F3 integration tests are structurally real but skipped without `TEST_DATABASE_ADMIN_URL`; gate result 1 prerequisite pass, 3 safe skips, 0 fail. F5/F6 lifecycle behavior is covered by real in-memory history/lifecycle suites and named tests; latest combined Phase 3/Phase 0 baseline 48/48 tests, 149 assertions; build and `git diff --check` pass. F9 explicitly deferred: the injected `Compactor`/`CompactionResult` port exposes no archive/provenance references, and production provenance population must be implemented with the Phase 4 producer without editing `src/compaction` in this round. Remaining gaps: only disposable PostgreSQL integration execution/operational validation; no migration, operational database, live API, commit, push, or deployment used. Remaining Phase 3 scope: PostgreSQL restart/integration execution and any final production operational validation only; implementation scope E/F is complete for the available seams.
- 2026-09-09: Phase 3 consolidated-review fix round (`integrity-lifecycle-20260908`): F6 durable compaction intent coverage added with real in-memory persistence: `request_compaction_coalesces_and_consume_is_idempotent` proves two requests create one receipt and one-consumption semantics; `compaction_intent_survives_restart_and_is_consumed_once` proves restart durability; exact `cache_state_publishes_only_after_commit` proves failed compaction does not reset/publish dynamic snapshot state. F8 durable completed-turn counter coverage added: `completed_turn_counter_is_durable_monotonic_and_typed` proves restart persistence, monotonic writes, and typed invalid-input failure; `resume_counter_and_interval_continuity_from_durable_lifecycle` proves an auto-resumed agent advances from durable count 2 to interval boundary 3. F10 remains implemented via atomic `saveAndPruneCheckpoint`; F11 remains implemented via idempotent agent shutdown drain; F12 remains implemented via persisted loop-detector warning messages; F13 remains implemented via best-effort completion projection reread that cannot discard a durable response; F14 coverage added as `cancel_mid_provider_call_surfaces_typed_cancelled`, proving ingress signal propagation and typed `CANCELLED` delivery during a provider call. F16 fixed: all integrity-lifecycle validation, recovery-marker, counter, and trusted-acknowledgement failures now throw `AgentError('INTEGRITY_FAILED')` with bounded context; no plain `Error` throws remain in `src/agent/integrity-lifecycle.ts`. Extended `src/testing/ports.ts` to model durable turn-counter upserts. Verification: new fix-round suite **7/7 tests, 20 assertions**; consolidated Phase 3/agent/Phase 0 command **123/123 tests, 410 assertions**; accepted model/rate-limit command **37/37 tests, 127 assertions**; `bun run build` pass; `git diff --check` pass. Required `src/agent/phase3.integration-required.test.ts` gate **1 prerequisite pass, 3 safe skips** because `TEST_DATABASE_ADMIN_URL` is unset; PostgreSQL history suite fake portion **14/14**, integration portion blocked at the same required prerequisite. No database, live API, migration, commit, push, or deployment used.
- 2026-09-09: Phase 3 delta-review correction (`integrity-lifecycle-20260908`): (a) loop-detector HALT now places the durable ID returned by `persistMessage` into the completion/checkpoint history copy, eliminating fabricated checkpoint IDs; `loop_halt_checkpoint_message_ids_are_durable` verifies every captured checkpoint ID resolves in durable history; (b) shutdown ownership is corrected: when `agentShutdown` is supplied, it drains the agent and owns the sole shutdown checkpoint, while the legacy direct `checkpointFn` is fallback-only; `agent-owned shutdown performs exactly one checkpoint capture` verifies one drain, zero legacy captures, and idempotent repeated shutdown; this corrects the earlier wording that described one shutdown capture without stating the ownership condition; (c) entry 85’s signal-propagation claim is corrected from an overstatement of completed coverage to the actual delta: caller signal/deadline are now threaded through `performRecall`/`decomposeMessage` into the decomposition `ModelRequest`, with `forwards caller signal and deadline to the decomposition request` coverage; (d) the stale duplicated trailing text in entry 82 is corrected by this entry and must not be interpreted as a second unimplemented-scope claim. Verification: focused delta tests **98/98 pass, 199 assertions**; broader Phase 3/Phase 0 and agent/checkpoint/history/outcome matrix **141 pass, 3 safe PostgreSQL skips, 0 fail, 464 assertions**; `bun run build` pass; `git diff --check` pass. PostgreSQL integration remains unavailable because `TEST_DATABASE_ADMIN_URL` is unset; no database, live API, migration, commit, push, or deployment used.
- 2026-09-09: Phase 5 MCP hardening (`integrity-lifecycle-20260908`, exclusive `src/mcp/**` lease): added `McpDiscoveryOptions` with caller signal, absolute whole-operation deadline (default 30 seconds), maxPages (default 64), repeated-cursor/page-cap guards, typed bounded discovery errors, and pure fake-clock `collectMcpPages`; MCP client forwards remaining timeout/signal/maxTotalTimeout to the installed SDK calls. Discovery now closes failed startup clients and `connectMcpServers` continues later servers with bounded visible summaries. Provider discovery builds immutable generation-tagged definitions/mappings off-side, rejects normalized-name collisions before publication, preserves prior generation on failure/stale attempt, and stale handlers fail explicitly. Full valid JSON input schemas are deep-frozen and retained; flat parameters remain advisory projection only; nested objects/arrays, unions and enums are retained, while references/assertion keywords not enforced by the accepted registry validator are rejected with precise tool/path diagnostics. MCP results preserve result-level `isError`, structured content, and bounded explicit text/image/audio/resource/resource-link descriptors; transport errors remain distinct and logs expose only bounded safe summaries. Added `publishMcpRegistrations` reservation/replacement helper for atomic root publication and `connectMcpServers` startup helper. Installed SDK verification evidence: `node_modules/@modelcontextprotocol/sdk/package.json` is version **1.29.0**; `dist/esm/client/index.d.ts` declares `connect(transport, options?: RequestOptions)`, `listTools/listPrompts/getPrompt` with final `RequestOptions`, and `callTool(params, resultSchema?, options?: RequestOptions)`; `dist/esm/shared/protocol.d.ts` declares `signal`, `timeout`, `maxTotalTimeout`; `dist/esm/validation/index.d.ts` exports only validator types, while `validation/ajv` exports a provider for schema validation (SDK output-oriented), so no direct JSON-schema dependency was added. Provider integration closure: N/A beyond this installed-signature reconciliation; no recorded fixture drift remained in the Phase 5 lease. Named MCP suite `src/mcp/phase5.test.ts` plus existing MCP suites: **117/117 tests, 250 assertions**. Accepted baseline command (Phase 0, model/provider, custom-tool registry): **64/64 tests, 218 assertions**. MCP-only TypeScript diagnostics: clean; `git diff --check -- src/mcp package.json src/config/schema.ts`: clean. Full `bun run build` remains blocked by unrelated off-lease Phase 4 error `src/compaction/durable.ts:82` (`makeRecursiveRequest` unused); no MCP diagnostics. Parent integration caveat: `src/index.ts` remains outside this lease and still needs to invoke the additive atomic/startup helpers instead of its existing per-definition registration loop. No config fields, package dependencies, migrations, database, live provider, commit, push, or deployment changed/used.
- 2026-09-09: Phase 5 final verification addendum: `publishMcpRegistrations` now quarantines any names installed before a later replacement failure, so an unexpected mid-publication error cannot leave a callable half-installed MCP batch. Final verification: `bun test src/mcp/phase5.test.ts src/mcp/client.test.ts src/mcp/provider.test.ts src/mcp/schema-mapper.test.ts src/mcp/startup.test.ts` **76/76 tests, 181 assertions**; accepted Phase 0/model/provider/custom-tool baseline **128/128 tests, 382 assertions**; `bun run build` **pass**; `git diff --check` **pass**. No additional config fields or dependencies were added; parent still owns composition-root invocation of the additive startup/publication helpers.
- 2026-09-09: Phase 4 Round 1 (`integrity-lifecycle-20260908`, exclusive `src/compaction/**` lease): added pure exchange grouping/projection with complete tool-call/result groups, stable causal order, true timestamp extrema, bounded metadata/omission markers; deterministic continuation derivation; CLOSED/OPEN/HALF_OPEN breaker with trusted status/reset; typed compaction result/provenance fields; and an opt-in durable runner using read/prepare → model summarize → one `commitCompaction` publication. Added `src/compaction/{breaker,continuation,grouping,durable,phase4-pure.test,compaction.integration-required.test}.ts`, extended compactor/types/index, and corrected the integration harness to use `src/testing/test-database.ts`/`teardown()`. Verification: focused compaction/prompt/scoring/pure command **115/115 tests, 274 assertions**; `git diff --check` pass; integration gate fails visibly at missing `TEST_DATABASE_ADMIN_URL` as required. This round did not claim the named fake empty/fit/deadline/write/ack matrices or real PostgreSQL execution.
- 2026-09-09: Phase 4 continuation Round 2 (`integrity-lifecycle-20260908`): durable runner now maps empty/non-text summaries to typed `summary_empty`, maps receipt/publication ambiguity to `history_state_unknown`, validates recursive summary requests through the same fit/deadline/retry path, retains initial source archives while preparing recursive replacement provenance, and uses one atomic commit plan. `src/index.ts` now passes the existing `historyStore` instance into `createCompactor`; `src/composition-seam.ts` exposes trusted compaction status/reset over an injected compactor only; a seam regression covers that surface. Verification: focused compaction command remains **115/115, 274 assertions** before the seam test addition; `git diff --check` pass; in-scope compaction compile diagnostics clean. Full build remains blocked by off-lease Phase 5 `src/mcp/**` errors; PostgreSQL gate remains blocked by absent `TEST_DATABASE_ADMIN_URL`. Remaining Phase 4 scope is the requested real fake-level matrix coverage and DB-backed fault/migration execution; no database, migration, live provider, commit, push, or deployment used.
- 2026-09-09: Phase 4 continuation Round 3 (`integrity-lifecycle-20260908`): added `src/compaction/durable-fake-matrix.test.ts` using the real `createCompactor` + injected fake `ModelProvider` + `createConversationHistoryStore(createInMemoryPersistence())` boundary. Named matrices now cover empty/non-text summaries across retry paths with zero mutation, pre-provider unfittable gating, cancellation/deadline/retry attempt bounds, rollback state reload, commit-ack reconciliation, and recursive replacement failure retaining all source rows. Production wiring is complete within the granted narrow lease: `src/index.ts` passes the existing `historyStore` to `createCompactor`; `src/composition-seam.ts` exposes trusted breaker status/reset only through an injected compactor. Verification: all compaction suites (`durable-fake-matrix`, `phase4-pure`, `compactor`, `prompt`, `scoring`) **122/122 tests, 309 assertions**; `src/persistence/conversation-history.test.ts` **14/14, 45 assertions**; `bun run build` pass; `git diff --check` pass. PostgreSQL integration gate: **3 scenarios pass, 1 reconciliation scenario blocked at setup** because `TEST_DATABASE_ADMIN_URL` is unset. Remaining scope is DB-backed six-stage fault, lost-ack, stale-revision, and migration/archive-protection execution only. No database, migration, live provider, commit, push, or deployment used.
- 2026-09-09: Phase 5 MCP review-fix round (`integrity-lifecycle-20260908`, GLM findings H1/H2/M2/L1/L2/L4; leases `src/mcp/{client,provider,schema-mapper,types}.ts`, `src/mcp/phase5.test.ts`, narrow H1/H2 extension `src/custom-tool/validation.ts` and `src/tool/registry.ts`): H1 reconciled registry-side JSON Schema validation to accept `integer` and type unions while rejecting all-unsupported types with precise diagnostics; H2 made a supplied full `inputSchema` the sole dispatch authority, preserving flat validation unchanged for schema-less legacy tools and validating integer/anyOf/union/numeric-enum values through the retained schema path. M2 added real SDK 1.29.0 `Client.connect/listTools` loopback coverage using Phase 0 `MockMcpTransport`, asserting caller signal and remaining timeout/maxTotalTimeout forwarding plus 30,000 ms/64-page defaults. L1 now distinguishes duplicate identical original names (`mcp_discovery_duplicate_tool`) from distinct normalized collisions. L2 chains the underlying fetch error as `cause` on `mcp_discovery_transport_error` and bounds repeated-cursor details to a 128-character prefix plus original length. L4 distinguishes thrown SDK `McpError` as `MCP protocol error` and corrects the protocol-error log label. Changed paths: `src/custom-tool/validation.ts`, `src/mcp/{client,phase5.test.ts,provider,types}.ts`; `src/tool/registry.ts` was inspected/reconciled without additional behavioral changes; `src/index.ts` and `src/mcp/startup.ts` were not touched by this worker (parallel wiring worker owns them). Verification: Phase 5 suite **17/17 tests, 47 assertions**; required focused MCP/custom-tool/registry/Phase 0 command **245/245 tests, 577 assertions** across 18 files; `bun run build` pass; `git diff --check` pass. No database, migration, live MCP server, live provider, commit, push, or deployment used. Remaining risk: concurrent wiring-worker changes to `src/index.ts`/`src/mcp/startup.ts` require parent integration review; PostgreSQL integration remains endpoint-gated per Phase 0 rail.
- 2026-09-09: Phase 5 wiring correction (`integrity-lifecycle-20260908`, leases `src/index.ts` MCP startup section, `src/index.wiring.test.ts`, and `src/mcp/startup.ts`): replaced composition-root per-server MCP registration with env-resolved client construction, `connectMcpServers` independent bounded startup, staged generation-tagged `discoverRegistrations`, and one `publishMcpRegistrations` call. Failed connections/discoveries are disconnected, reported in the visible summary, and excluded from publication; successful clients retain prompt/instruction handling and shutdown ownership. Added wiring tests with injected fake clients proving two healthy servers publish complete registrations and first-server failure leaves only the second server’s tool published. Addressed GLM M3: rollback quarantine now catches each quarantine failure and guarantees release of all reserved names in `finally`; added `mcp_publication_rollback_quarantine` proving mid-publication failure quarantines installed names, releases every reservation, and continues cleanup despite a quarantine exception. Verification: `bun test src/index.wiring.test.ts` **63/63 tests, 117 assertions**; MCP suites including `src/mcp/phase5.test.ts` **77/77 tests, 185 assertions**; `bun test src/testing/phase0.test.ts` **20/20 tests, 55 assertions**; `bun run build` **pass**; `git diff --check` **pass**. Tests used only injected fakes; no real MCP servers or secret-bearing configuration were started/read. No migrations, database, live provider, commit, push, or deployment used.
- 2026-09-09: Phase 5 verification correction: strengthened the omitted-options M2 regression to execute `collectMcpPages` with no `maxPages` and prove the default cap stops exactly at 64 pages; `src/mcp/phase5.test.ts` remains **17/17 tests, 49 assertions** and `git diff --check` passes. A subsequent tree-wide `bun run build` was attempted and is blocked by unrelated concurrent Phase 4 errors in `src/compaction/compactor.ts` (`CompactionDurabilityRequiredError` unused) and `src/compaction/durable.ts` (`recoveryNote` type mismatch plus unused `errorCode`/`priorOperationId`); no off-lease files were changed. The prior successful build was recorded in the preceding Phase 5 review-fix entry.
- 2026-09-09: Phase 5 M3 micro-fix (`integrity-lifecycle-20260908`, exclusive lease `src/mcp/startup.ts`, `src/mcp/phase5.test.ts`, and this ledger): verified rollback catches each quarantine/release failure, always attempts all releases, and surfaces a typed `AggregateError` containing the publication cause plus cleanup failures; strengthened `mcp_publication_rollback_quarantine` to assert the aggregate/cause and cleanup messages, quarantined first name, release of both reservations, empty published registry, and preserved provider generation/handler. Composition-root wiring was completed separately by the `src/index.ts` worker: atomic publication and startup wiring tests **63/63 assertions**. Verification: MCP suites (`phase5`, `client`, `provider`, `schema-mapper`, `startup`) **84/84 tests, 212 assertions**; Phase 0 **20/20 tests, 55 assertions**; `bun run build` pass; `git diff --check` pass. No compaction noise occurred in this build; no database, migration, live server/provider, commit, push, or deployment used.
- 2026-09-09: Phase 5 delta micro-fix (`integrity-lifecycle-20260908`, leases `src/custom-tool/validation.ts`, `src/mcp/schema-mapper.ts`, `src/mcp/phase5.test.ts`, and this ledger): M-1 restored flat-path enum diagnostics to `invalid value for parameter <name>: expected one of [<values>], got <value>`, satisfying search AC1.7; L-1 added `allOf`, `patternProperties`, `prefixItems`, and `dependencies` to the fail-closed unsupported-keyword set and removed `allOf` from accepted recursive unions. Added one discovery regression per keyword asserting the precise `$.<keyword>` diagnostic, unchanged generation 1, and continued prior handler usability. Verification: affected search/custom-tool/MCP command **153/153 tests, 410 assertions**; full non-DB custom-tool plus MCP command **189/189 tests, 433 assertions**; Phase 0 **28/28 tests, 73 assertions**; `git diff --check` pass. `bun run build` is blocked by off-lease parallel compaction syntax error `src/compaction/compactor.ts:745` (`TS1128`); no compaction files were inspected or changed. No database, migration, live server/provider, commit, push, or deployment used.
- 2026-09-09: Phase 4 consolidated-review remediation (`integrity-lifecycle-20260908`, leased compaction plus minimal agent/config/root/seam/history extensions): H1 fixed by consuming durable `CompactionResult.operationId`, `archiveIds`, `provenanceRefs`, and `revision` into agent checkpoint state; explicit-restore and auto-resume seed both provenance arrays, and durable restore remains exact-history-first. H2 fixed by reading the active clip summary and prior operation lineage into the next durable summarization request, with recursive summaries carrying `supersedesOperationId`. H3/L1 fixed by wiring summarization `context_window`, `safety_margin`, and additive `cooldown_ms` through production compaction config. M1 fixed by physically removing the legacy destructive no-store implementation; callers without `historyStore` receive typed `durability_required`, and retired destructive expectations are explicitly skipped in the legacy test file. M2–M5 fixed with real fake-level durable matrices using the real compactor/history-store boundary: empty/non-text output, pre-provider fit refusal, one-operation deadline with bounded exponential backoff, cancellation, typed stale revision/membership branching and intervention latch, commit/publication receipt handling, recursive replacement source retention, and source enumeration exchange-boundary protection. L4/L5/L6 fixed with recursive supersession lineage, trusted seam breaker status/reset, and dual-mode semantics documentation. Verification: `bun run build` pass; all compaction tests **93 pass, 35 intentional skips, 0 fail, 218 assertions** (the skips are retired destructive no-store cases and the PostgreSQL gate without its prerequisite); `bun test src/persistence/conversation-history.test.ts` **14/14, 45 assertions**; `bun test src/testing/phase0.test.ts` **20/20, 55 assertions**; `git diff --check` pass; production compaction tree has no transcript `DELETE FROM messages` path and integration file has no `expect(true)` placeholders. The new PostgreSQL gate contains four real scenarios (successful commit, rollback/reload retention, recursive failure retention, stale revision) and safely skips when `TEST_DATABASE_ADMIN_URL` is absent; real PostgreSQL execution remains pending that endpoint. No operational database, migration, live provider, commit, push, or deployment used.
- 2026-09-09: Phase 4 evidence-completion (`integrity-lifecycle-20260908`, exclusive leases `src/compaction/**`, `src/agent/checkpoint-triggers.test.ts`, and minimal `src/index.ts`/composition seam): added M-a `captures durable archive/provenance refs and restores the archived v2 summary`; M-b `two_cycle_durable_history_carries_prior_clip_and_supersedes_lineage`; M-c wired-config fit gate evidence via `summary_fit_matrix_single_cycle_gate`; M-d `committed_publication_failed_records_success_and_recovery_note` plus `history_stale_membership_maps_to_intervention_latch`; M-e `summary_empty_output_matrix_initial_recursive` and `summary_fit_matrix_initial_recursive` execute recursive legs with matching treatment; L-a `summary_causal_order_and_span` and boundary-aware durable source enumeration; L-b captured `supersedesOperationId` on the publication plan; L-c `L-c serialized recovery action is seam-only and model-free`; L-d `compaction_deadline_cancel_retry_matrix` proves fake-clock timeout during retry wait with bounded attempts. Focused evidence command passed **28/28 tests, 117 assertions** across durable matrix/checkpoint/pure suites; all compaction suites passed **100 pass, 35 intentional skips, 0 fail, 245 assertions**; `phase3-slice4` passed **10/10, 30 assertions**; Phase 0 transaction slice passed **6/6, 14 assertions**; history baseline passed **14/14, 45 assertions**; `bun run build` and `git diff --check` passed. PostgreSQL compaction integration remains safely skipped because `TEST_DATABASE_ADMIN_URL` is unset; no database, migration, live provider, commit, push, or deployment used.
- 2026-09-09: Phase 6 documentation lane completed on `remediation/integrity-lifecycle-20260908`. Changed documentation paths: `docs/implementation-plans/2026-09-08-remediation-operator-guide.md`, `docs/2026-09-08-codebase-review-and-recommendations.md` (original provenance/content preserved; additive R01–R17/P2 resolution matrix), `config.toml.example`, `AGENTS.md`, `src/memory/CLAUDE.md`, `src/runtime/CLAUDE.md`, `src/agent/CLAUDE.md`, `src/persistence/CLAUDE.md`, `src/compaction/CLAUDE.md`, `src/model/CLAUDE.md`, `src/mcp/CLAUDE.md`, and `src/custom-tool/CLAUDE.md`. Verified cited source defaults, commands, and regression names; `bun run build` and `git diff --check` passed. Documentation claims are source/unit/fake-test checked; PostgreSQL execution remains gated by absent `TEST_DATABASE_ADMIN_URL`, and no live provider, deployment, incident, migration, or production operation was reproduced or performed.

- 2026-09-09: Phase 6 integrated validation (`remediation/integrity-lifecycle-20260908`; read-only validation plus additive named-test completion): added `src/remediation.e2e.integration-required.test.ts`, exercising the real composition seam (`createCompositionSeam().createAgent`), registry, history store, compactor, and integrity lifecycle across concurrent ingress, complete tool batch, deferred `compact_context`, durable compaction, checkpoint capture, startup auto-resume, fresh persistence-instance selection, and explicit archive-aware restore. The fake equivalent passes; PostgreSQL execution remains explicitly gated. Added exact named coverage without production changes: `manual_compaction_waits_for_complete_batch` and `checkpoint_restore_commit_ack_reconciliation`; split `tool_outcome_database_roundtrip` and `legacy_outcome_no_substring_inference`; renamed the existing memory race to `memory_delete_permission_race`; added PostgreSQL-gated `archivist_preserves_history_owned_archives` and `checkpoint_restore_archive_content_survives_maintenance`.

  **Command evidence (database variable explicitly unset):** `bun run build` PASS (`tsc --noEmit`); `deno check src/runtime/deno/runtime.ts` PASS; `bun run test:unit` PASS (**28/28 tests, 73 assertions**); final fake matrix (`bun test src/agent/phase3-fix-round.test.ts src/agent/phase3-slice4.test.ts src/agent/phase3-slice5.test.ts src/phase3-consolidated-review.test.ts src/persistence/conversation-history.test.ts src/compaction/durable-fake-matrix.test.ts src/compaction/phase4-pure.test.ts`) PASS (**56/56, 198 assertions**); split AC.7 suite (`bun test src/agent/phase3-slice4.test.ts`) PASS (**11/11, 31 assertions**); `git diff --check` PASS. `env -u TEST_DATABASE_ADMIN_URL bun run test:integration-required` failed visibly as required (**3 pass, 10 skips, 8 prerequisite failures across 21 tests**). `env -u TEST_DATABASE_ADMIN_URL bun run test:legacy-isolated` failed (**0 pass, 33 fail across 18 files**) at expected missing-DB setup and the pre-existing `teardownTestDatabase` undefined `database.databaseName` defect in `src/subconscious/persistence.test.ts` and `src/search/domains/memory.test.ts`; no database was created. `env -u TEST_DATABASE_ADMIN_URL bun run test:full-isolated` passed the unit stage, then failed at `src/persistence/tx-nesting.test.ts` on the missing-DB prerequisite and therefore did not reach legacy or integration-required stages. The standalone consolidated E2E check reported its two fake tests passing, one PostgreSQL test skipped, and one explicit prerequisite failure.

  **Every integration-required file with `TEST_DATABASE_ADMIN_URL` unset:** `src/testing/integration-required.test.ts` (**0 pass, 1 fail**); `src/persistence/transaction-ac21.integration-required.test.ts` (**0/1 fail**); `src/memory/memory-delete.integration-required.test.ts` (**0/1 fail**); `src/persistence/conversation-history.integration-required.test.ts` (**0/1 fail**); `src/custom-tool/custom_tool_commit_ack_reconciliation.integration-required.test.ts` (**1 pass, 1 fail**); `src/agent/phase3.integration-required.test.ts` (**0 pass, 3 skips, 1 fail**); `src/compaction/compaction.integration-required.test.ts` (**0 pass, 6 skips, 1 fail**); `src/remediation.e2e.integration-required.test.ts` (**2 pass, 1 skip, 1 fail**). All failures were explicit prerequisite failures; no operational/default database or live API was contacted. The phase-3 and compaction gates now have standalone prerequisite assertions so they fail nonzero instead of silently succeeding with skips. `package.json` `test:integration-required` wires all eight files; the required suites are intentionally not placed in `test:legacy-isolated`.

  **AC.1–AC.21 audit:**
  | AC | Named tests | Status |
  |---|---|---|
  | AC.1 | `test_db_rejects_unsafe_target`; `test_db_teardown_only_owned_database`; `integration_mode_requires_database` | Safe fake tests pass; required DB mode fails visibly without env. |
  | AC.2 | `memory_delete_authorization_matrix`; `memory_delete_permission_race`; `maintenance_owner_tier_boundaries` | Fake/policy evidence passes; real PostgreSQL suite is env-gated, not executed. |
  | AC.3 | `queued_host_call_never_starts_after_timeout`; `cancelled_execution_cleanup_is_bounded`; `late_host_completion_is_observed_only` | Pass in runtime regression suite. |
  | AC.4 | `runtime_raw_stream_budget_matrix`; `runtime_utf8_budget`; `runtime_terminal_exit_frame_race` | Pass in runtime regression suite. |
  | AC.5 | `custom_metadata_rejection_matrix`; `registry_reservation_commit_failure`; `invalid_persisted_tool_quarantine`; `generated_script_parse_matrix` | Pass in focused fake suites. |
  | AC.6 | `mixed_ingress_serializes_complete_turns`; `queue_failure_and_cancel_release`; `incomplete_batch_blocks_future_provider_calls` | Pass with real agent/registry and transactional in-memory persistence. |
  | AC.7 | `exchange_shaping_protocol_matrix`; `registry_failures_reach_next_model_request`; `tool_outcome_database_roundtrip`; `legacy_outcome_no_substring_inference` | Pass; the final two are now separate exact tests. |
  | AC.8 | `agent_checkpoint_real_trigger_matrix`; `resume_counter_and_interval_continuity`; `interval_save_failure_does_not_fail_turn` | Fake trigger matrix passes; PostgreSQL execution not run. |
  | AC.9 | `live_write_checkpoint_restore_compatibility`; `checkpoint_version_and_missing_source_matrix`; `restore_failure_has_no_partial_state` | Fake/real-store seam tests pass; DB-backed legacy restore remains gated. |
  | AC.10 | `compaction_write_failure_reload_matrix`; `recursive_replacement_failure_retains_sources`; `compaction_stale_revision`; `compaction_commit_ack_reconciliation` | Fake durable matrix passes; PostgreSQL fault/reload/ack scenarios are env-gated. |
  | AC.11 | `precompaction_checkpoint_exact_restore_after_restart`; `history_projection_migration_preserves_rows`; `checkpoint_prune_retains_transcript` | Fake/history retention evidence passes; real migration/restore scenarios are env-gated. |
  | AC.12 | `summary_empty_output_matrix_initial_recursive` | Fake recursive matrix passes. |
  | AC.13 | `breaker_open_half_open_recovery`; `breaker_permanent_fault_requires_reset` | Pass. |
  | AC.14 | `agent_actual_context_pressure_matrix` (added in this consolidated round); `manual_compaction_waits_for_complete_batch`; `summary_fit_matrix_initial_recursive`; `compaction_deadline_cancel_retry_matrix` | Fake evidence passes; exact manual scenario added and passes. |
  | AC.15 | `summary_causal_order_and_span`; `continuation_preserves_objective_and_tool_status` | Pass. |
  | AC.16 | `mcp_discovery_bound_matrix`; `mcp_generation_collision_atomicity`; `mcp_nested_schema_and_result_semantics`; `startup_skips_failed_server_continues_next` | Pass in Phase 5 suite. |
  | AC.17 | `provider_wire_contract_matrix`; `provider_terminal_usage_matrix`; `provider_lifetime_cancellation_matrix`; `usage_accounting_no_double_count` | Pass in accepted provider suites. |
  | AC.18 | `cache_state_publishes_only_after_commit`; `remediation_end_to_end_restart_scenario` | Fake production-seam evidence passes; PostgreSQL restart execution is env-gated. |
  | AC.19 | `auto_resume_preserves_post_checkpoint_commits`; `auto_resume_after_committed_compaction`; `restart_with_unfinished_tool_batch_never_replays_effects` | Fake/seam evidence passes; production PostgreSQL startup execution is env-gated. |
  | AC.20 | `retained_archive_rejects_public_mutation`; `archivist_preserves_history_owned_archives`; `checkpoint_restore_archive_content_survives_maintenance` | All three are real PostgreSQL tests in the gated history suite; none executed without env. |
  | AC.21 | `transaction_context_does_not_cross_provider_instances`; `transaction_outcome_preserves_commit_error`; `custom_tool_commit_ack_reconciliation`; `checkpoint_restore_commit_ack_reconciliation`; `compaction_commit_ack_reconciliation` | Transaction/custom/compaction fake evidence passes; exact restore-ack test passes against real in-memory history store; PostgreSQL receipt reconciliation remains env-gated. |

  **DB-blocked evidence and defects:** AC.14 was previously reported without its named agent-level matrix; this round corrects that ledger disposition. No `TEST_DATABASE_ADMIN_URL` was available, so no PostgreSQL migration, row-lock race, archive trigger, fresh-PostgreSQL restart, transaction fault, receipt reconciliation, or real maintenance/archive scenario ran. No live provider/API, operational database, migration command, deployment, stage, commit, push, or merge was performed. The only pre-existing defect found during the sweep is the legacy-suite teardown initialization error (`teardownTestDatabase` called with undefined state), outside this lease and unchanged. The phase-3/compaction gate design defect (zero exit with only skips) was corrected by explicit prerequisite tests. Remaining risk is unavailable PostgreSQL evidence plus the pre-existing legacy harness defect; fake, Deno, type-check, and named-test evidence is complete for the audited names.

  **2026-09-09 — final consolidated review disposition (append-only):** H1 exchange shaping is wired into live agent admission with typed `EXCHANGE_CORRUPT`; trusted recovery repairs crash-orphaned tool results and all unfinished batches. M1/M2/M3 persistence truth is fail-closed for failed rollback, missing receipts, nested compaction, and reconciled commits without values; restore archive references, operation-scoped provenance, position `MAX+1`, state upsert, and timestamp-safe fake seams are implemented. M4 breaker probes are released on no-op compaction; M5 has real agent context-pressure coverage. L1, L3 quarantine guard, L5 transient classification, L6 max-round checkpoint consumption, L10 ambiguity surfacing, and L12 all-batch recovery are complete. Deliberately deferred: L2 BEGIN broken-flag, L3 savepoint command-tag validation, L7 dead GUC mechanism, L8 idempotent-replay plan-equivalence trade-off, L9 migration-regex note, and PERSISTENCE-L4/L6 labeling/context-size retry tension; the bounded three-attempt mitigation remains in force. Remaining evidence risk is PostgreSQL integration/maintenance coverage blocked by absent `TEST_DATABASE_ADMIN_URL`.

  **2026-09-09 — closing micro-round (append-only; lease: `src/testing/ports.ts`, `src/persistence/conversation-history-store.ts`, `src/persistence/conversation-history.test.ts`, `src/phase3-consolidated-review.test.ts`):** MEDIUM-1 closed with `restore_provenance_remains_compactable_after_exact_restore`: fake provenance records receipt-derived `operation_type`, compaction-source enumeration excludes only committed compaction sources, and exact-restored prefix is enumerated then durably compacted through the real history store. LOW-1 closed with `repair_orphaned_tool_result_then_recovery_allows_next_agent_turn`: `recover()` directly exercises orphan repair, appends `outcome_unknown`/`trusted_backfill`, completes the batch receipt, and a subsequent real agent turn completes with one provider call. LOW-2 closed with `history_mutations_reject_nested_transaction_scopes_with_typed_codes`: both history operations expose `history_outermost_required` via `defineProperty` in nested provisional scopes. LOW-3 closed by replacing fake `String(created_at)` ordering with explicit timestamp milliseconds for historical messages, superseded messages, and checkpoint pruning. Verification: `bun run build` passed; touched suites passed (22 tests); phase-0/focused compaction-history suites passed (45 tests); `git diff --check` passed. No PostgreSQL integration was run because `TEST_DATABASE_ADMIN_URL` was unavailable; no migration, deployment, push, merge, or operational database mutation was performed.
